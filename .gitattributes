# The working copy lives on a Windows drive; the repository stays LF.
* text=auto eol=lf
*.png binary

# Not our code: keep it out of the language statistics and collapse it in diffs.
qrcode.js linguist-vendored
